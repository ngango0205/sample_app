<%= link_to gravatar_for(current_user, size = Settings.size.avar_size), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t(".view_my_pf"), current_user %></span>
<span><%= pluralize(current_user.microposts.count, t(".microposts")) %></span>
